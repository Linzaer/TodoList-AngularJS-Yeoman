'use strict';

/**
 * @ngdoc function
 * @name currentlyApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the currentlyApp
 */
angular.module('currentlyApp')
  .controller('UserCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
